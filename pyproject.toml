[project]
name = "reloaded"
version = "0.1.0"
description = "A Windows Terminal launcher for Claude Code."
requires-python = ">=3.9"
dependencies = [
    "psutil",
    "uiautomation",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
